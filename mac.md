reset:
```
sudo bash -lc $'cat > /usr/local/bin/reset-openjarvis <<\\\'SH\\\'\n#!/usr/bin/env bash\nset -euo pipefail\nTS=$(date +%Y%m%d_%H%M%S)\nREMOVED_DIR=\"$HOME/openjarvis_removed_$TS\"\nmkdir -p \"$REMOVED_DIR\"\necho \"reset-openjarvis: staging to $REMOVED_DIR\"\n# stop processes (best-effort)\nif [ -f \"$HOME/openjarvis-voice.pid\" ]; then\n  PID=$(cat \"$HOME/openjarvis-voice.pid\" 2>/dev/null || true)\n  if [ -n \"$PID\" ]; then kill \"$PID\" 2>/dev/null || true; fi\n  rm -f \"$HOME/openjarvis-voice.pid\" || true\nfi\npkill -f openjarvis_voice.py 2>/dev/null || true\npkill -f openjarvis_voice_wrapper.sh 2>/dev/null || true\nsleep 0.3\n# move known candidates to staging\nfor p in \"$HOME/Desktop/openjarvis\" \"$HOME/openjarvis\" \"$HOME/openjarvis*\" \"$HOME/OpenJarvis*\" \"$HOME/OPENJARVIS*\" \"$HOME/openjarvis_voice*\" \"$HOME/start_with_voice.sh\" \"$HOME/stop_with_voice.sh\" \"$HOME/start_foreground_with_voice.sh\" \"$HOME/openjarvis_voice_wrapper.sh\" \"$HOME/openjarvis_voice_wake_patch.py\" \"$HOME/.openjarvis_voice_venv\" \"$HOME/openjarvis-voice.log\" \"$HOME/openjarvis-voice.pid\"; do\n  for f in $p; do\n    [ -e \"$f\" ] || continue\n    mv -v \"$f\" \"$REMOVED_DIR/\" 2>/dev/null || true\n  done\ndone\n# permanently remove staged items\nif [ -d \"$REMOVED_DIR\" ] && [ \"$REMOVED_DIR\" != \"/\" ] && [ \"$REMOVED_DIR\" != \"$HOME\" ]; then\n  rm -rf \"$REMOVED_DIR\"\n  echo \"reset-openjarvis: removed $REMOVED_DIR\"\nelse\n  echo \"reset-openjarvis: staging dir missing or unsafe, skipped permanent delete\"\nfi\n# final cleanup\nfor f in \"$HOME/openjarvis-voice.log\" \"$HOME/openjarvis-voice.pid\" \"$HOME/start_with_voice.sh\" \"$HOME/stop_with_voice.sh\" \"$HOME/start_foreground_with_voice.sh\" \"$HOME/openjarvis_voice_wrapper.sh\"; do\n  [ -e \"$f\" ] && rm -rf \"$f\" 2>/dev/null || true\ndone\necho \"reset-openjarvis: complete\"\nSH\nchmod +x /usr/local/bin/reset-openjarvis'
```

