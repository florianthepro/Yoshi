reset:
```
bash -lc 'pkill -f openjarvis_voice.py 2>/dev/null || true; pkill -f openjarvis_voice_wrapper.sh 2>/dev/null || true; sleep 0.2; rm -rf "$HOME/Desktop/openjarvis" "$HOME/openjarvis" "$HOME/openjarvis_voice.py" "$HOME/openjarvis-voice.log" "$HOME/openjarvis-voice.pid" "$HOME/start_with_voice.sh" "$HOME/stop_with_voice.sh" "$HOME/start_foreground_with_voice.sh" "$HOME/openjarvis_voice_wrapper.sh" "$HOME/openjarvis_voice_wake_patch.py" "$HOME/.openjarvis_voice_venv"; echo "reset-openjarvis: done"'
```
```
bash -lc 'set -euo pipefail; T="$HOME/Desktop/openjarvis"; if [ -e "$T" ] && [ -n "$(ls -A "$T" 2>/dev/null)" ]; then T="${T}-$(date +%Y%m%d_%H%M%S)"; fi; echo "Installing OpenJarvis into: $T"; git clone https://github.com/open-jarvis/OpenJarvis.git "$T" >/dev/null 2>&1 || (echo "Clone failed or repo exists, attempting to use existing folder $T"; mkdir -p "$T"); cd "$T"; echo "Ensure Homebrew..."; if ! command -v brew >/dev/null 2>&1; then echo "Installing Homebrew (may prompt)"; /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"; fi; echo "Ensure Node..."; if ! command -v node >/dev/null 2>&1; then brew install node >/dev/null 2>&1 || true; fi; echo "Ensure Rust..."; if ! command -v rustc >/dev/null 2>&1; then curl --proto "=https" --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y; source "$HOME/.cargo/env" || true; fi; echo "Ensure uv (astral) if repo uses it..."; if ! command -v uv >/dev/null 2>&1; then curl -LsSf https://astral.sh/uv/install.sh | sh || true; fi; echo "Create isolated venv..."; python3 -m venv "$T/venv"; "$T/venv/bin/python" -m pip install --upgrade pip wheel setuptools >/dev/null 2>&1 || true; if [ -f "requirements.txt" ]; then echo "Installing Python requirements..."; "$T/venv/bin/python" -m pip install -r requirements.txt || true; fi; if [ -d "frontend" ]; then echo "Installing frontend deps (npm)..."; if command -v npm >/dev/null 2>&1; then (cd frontend && npm install) || true; else echo "npm missing; frontend not installed"; fi; fi; echo "Create start.sh and stop.sh (isolated)"; printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "cd \"\$BASE_DIR\"" "VENV=\"\$BASE_DIR/venv\"" "LOG=\"\$BASE_DIR/openjarvis.log\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "echo \"Starting OpenJarvis backend...\"" "if command -v uv >/dev/null 2>&1; then nohup uv run jarvis serve --port 8000 >>\"\$LOG\" 2>&1 & echo \$! >\"\$PIDF\"; else nohup \"\$VENV/bin/python\" -m jarvis serve --port 8000 >>\"\$LOG\" 2>&1 & echo \$! >\"\$PIDF\"; fi" "if [ -d \"\$BASE_DIR/frontend\" ] && command -v npm >/dev/null 2>&1; then (cd frontend && nohup npm run dev >>\"\$LOG\" 2>&1 &) ; fi" "sleep 1" "open \"http://127.0.0.1:5173/\" || true" "echo \"Started. Backend PID: \$(cat \\\"\$PIDF\\\")\"" > "$T/start.sh"; chmod +x "$T/start.sh"; printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "echo \"Stopping OpenJarvis...\"" "if [ -f \"\$PIDF\" ]; then kill \"\$(cat \"\$PIDF\")\" 2>/dev/null || true; rm -f \"\$PIDF\"; fi" "pkill -f \"npm run dev\" 2>/dev/null || true" "pkill -f \"uv run jarvis\" 2>/dev/null || true" "echo \"Stopped.\"" > "$T/stop.sh"; chmod +x "$T/stop.sh"; echo "Installation complete. Start with: $T/start.sh ; Stop with: $T/stop.sh"'
```
