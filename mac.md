reset:
```
bash -lc 'pkill -f openjarvis_voice.py 2>/dev/null || true; pkill -f openjarvis_voice_wrapper.sh 2>/dev/null || true; sleep 0.2; rm -rf "$HOME/Desktop/openjarvis" "$HOME/openjarvis" "$HOME/openjarvis_voice.py" "$HOME/openjarvis-voice.log" "$HOME/openjarvis-voice.pid" "$HOME/start_with_voice.sh" "$HOME/stop_with_voice.sh" "$HOME/start_foreground_with_voice.sh" "$HOME/openjarvis_voice_wrapper.sh" "$HOME/openjarvis_voice_wake_patch.py" "$HOME/.openjarvis_voice_venv"; echo "reset-openjarvis: done"'
```
```
bash -lc 'TARGET="$HOME/Desktop/openjarvis"; if [ -e "$TARGET" ] && [ -n "$(ls -A "$TARGET" 2>/dev/null)" ]; then TARGET="${TARGET}-$(date +%Y%m%d_%H%M%S)"; fi; mkdir -p "$TARGET/data" "$TARGET/venv" && python3 -m venv "$TARGET/venv" && printf "%s\n" "#!/usr/bin/env python3" "print(\"OpenJarvis placeholder (isolated install). Replace data/openjarvis_voice.py with your real script.\")" > "$TARGET/data/openjarvis_voice.py" && chmod +x "$TARGET/data/openjarvis_voice.py" && printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "DATA_DIR=\"\$BASE_DIR/data\"" "VENV_DIR=\"\$BASE_DIR/venv\"" "OPENVOICE=\"\$DATA_DIR/openjarvis_voice.py\"" "LOG=\"\$BASE_DIR/openjarvis.log\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "echo \"To run: \$VENV_DIR/bin/python \$OPENVOICE (this script is a placeholder)\"" > "$TARGET/start_with_voice.sh" && chmod +x "$TARGET/start_with_voice.sh" && printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "if [ -f \"\$PIDF\" ]; then kill \"\$(cat \"\$PIDF\")\" 2>/dev/null || true; rm -f \"\$PIDF\"; fi" > "$TARGET/stop_with_voice.sh" && chmod +x "$TARGET/stop_with_voice.sh" && echo "install-openjarvis: created at $TARGET"'
```
