reset:
```
bash -lc 'pkill -f openjarvis_voice.py 2>/dev/null || true; pkill -f openjarvis_voice_wrapper.sh 2>/dev/null || true; sleep 0.2; rm -rf "$HOME/Desktop/openjarvis" "$HOME/openjarvis" "$HOME/openjarvis_voice.py" "$HOME/openjarvis-voice.log" "$HOME/openjarvis-voice.pid" "$HOME/start_with_voice.sh" "$HOME/stop_with_voice.sh" "$HOME/start_foreground_with_voice.sh" "$HOME/openjarvis_voice_wrapper.sh" "$HOME/openjarvis_voice_wake_patch.py" "$HOME/.openjarvis_voice_venv"; echo "reset-openjarvis: done"'
```
```
bash -lc 'set -euo pipefail; T="$HOME/Desktop/openjarvis"; if [ -e "$T" ] && [ -n "$(ls -A "$T" 2>/dev/null)" ]; then T="${T}-$(date +%Y%m%d_%H%M%S)"; fi; echo "Install path: $T"; git clone https://github.com/open-jarvis/OpenJarvis.git "$T" >/dev/null 2>&1 || (echo "Using existing $T"; mkdir -p "$T"); cd "$T"; echo "Ensure Homebrew, Node, Rust, uv..."; if ! command -v brew >/dev/null 2>&1; then /bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\"; fi; if ! command -v node >/dev/null 2>&1; then brew install node >/dev/null 2>&1 || true; fi; if ! command -v rustc >/dev/null 2>&1; then curl --proto \"=https\" --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y; source \"$HOME/.cargo/env\" || true; fi; if ! command -v uv >/dev/null 2>&1; then curl -LsSf https://astral.sh/uv/install.sh | sh || true; fi; echo "Create isolated venv (.venv) and install Python deps via uv..."; python3 -m venv .venv || true; . .venv/bin/activate; pip install --upgrade pip wheel setuptools >/dev/null 2>&1 || true; uv sync --extra server || true; echo "Build Rust extension (best-effort)"; uv run maturin develop -m rust/crates/openjarvis-python/Cargo.toml || true; if [ -d "frontend" ]; then (cd frontend && npm install) || true; fi; echo "Writing start/stop scripts..."; printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "cd \"\$BASE_DIR\"" "LOG=\"\$BASE_DIR/openjarvis.log\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "echo Starting backend..." "nohup uv run jarvis serve --port 8000 >>\"\$LOG\" 2>&1 & echo \$! >\"\$PIDF\" || (nohup ./.venv/bin/python -m jarvis serve --port 8000 >>\"\$LOG\" 2>&1 & echo \$! >\"\$PIDF\")" "if [ -d \"\$BASE_DIR/frontend\" ]; then (cd frontend && nohup npm run dev >>\"\$LOG\" 2>&1 &) ; fi" "sleep 1" "open \"http://127.0.0.1:5173/\" || true" "echo Started (pid \$(cat \"\$PIDF\"))" > "$T/start.sh"; chmod +x "$T/start.sh"; printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "echo Stopping..." "if [ -f \"\$PIDF\" ]; then kill \"\$(cat \"\$PIDF\")\" 2>/dev/null || true; rm -f \"\$PIDF\"; fi" "pkill -f \"npm run dev\" 2>/dev/null || true" "pkill -f \"uv run jarvis\" 2>/dev/null || true" "echo Stopped" > "$T/stop.sh"; chmod +x "$T/stop.sh"; echo "Done. Start: $T/start.sh  Stop: $T/stop.sh"'
```
