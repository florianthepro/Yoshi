reset:
```
bash -lc 'pkill -f openjarvis_voice.py 2>/dev/null || true; pkill -f openjarvis_voice_wrapper.sh 2>/dev/null || true; sleep 0.2; rm -rf "$HOME/Desktop/openjarvis" "$HOME/openjarvis" "$HOME/openjarvis_voice.py" "$HOME/openjarvis-voice.log" "$HOME/openjarvis-voice.pid" "$HOME/start_with_voice.sh" "$HOME/stop_with_voice.sh" "$HOME/start_foreground_with_voice.sh" "$HOME/openjarvis_voice_wrapper.sh" "$HOME/openjarvis_voice_wake_patch.py" "$HOME/.openjarvis_voice_venv"; echo "reset-openjarvis: done"'
```
```
bash -lc 'T="$HOME/Desktop/openjarvis"; if [ -e "$T" ] && [ -n "$(ls -A "$T" 2>/dev/null)" ]; then T="${T}-$(date +%Y%m%d_%H%M%S)"; fi; mkdir -p "$T/data" "$T/venv"; python3 -m venv "$T/venv" >/dev/null 2>&1 || true; "$T/venv/bin/python" -m pip install --upgrade pip >/dev/null 2>&1 || true; printf "%s\n" "#!/usr/bin/env python3" "print(\"OpenJarvis isolated placeholder\")" > "$T/data/openjarvis_voice.py"; chmod +x "$T/data/openjarvis_voice.py"; printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "VENV_DIR=\"\$BASE_DIR/venv\"" "DATA_FILE=\"\$BASE_DIR/data/openjarvis_voice.py\"" "nohup \"\$VENV_DIR/bin/python\" \"\$DATA_FILE\" >/dev/null 2>&1 & echo \$! > \"\$BASE_DIR/openjarvis.pid\"" > "$T/start_with_voice.sh"; chmod +x "$T/start_with_voice.sh"; printf "%s\n" "#!/usr/bin/env bash" "BASE_DIR=\"\$(cd \"\$(dirname \"\$0\")\" && pwd)\"" "PIDF=\"\$BASE_DIR/openjarvis.pid\"" "if [ -f \"\$PIDF\" ]; then kill \"\$(cat \"\$PIDF\")\" 2>/dev/null || true; rm -f \"\$PIDF\"; else pkill -f \"\$BASE_DIR/data/openjarvis_voice.py\" 2>/dev/null || true; fi" > "$T/stop_with_voice.sh"; chmod +x "$T/stop_with_voice.sh"; echo "install-openjarvis: created at $T"'
```
