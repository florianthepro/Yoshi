reset:
```
bash -lc 'pkill -f openjarvis_voice.py 2>/dev/null || true; pkill -f openjarvis_voice_wrapper.sh 2>/dev/null || true; sleep 0.2; rm -rf "$HOME/Desktop/openjarvis" "$HOME/openjarvis" "$HOME/openjarvis_voice.py" "$HOME/openjarvis-voice.log" "$HOME/openjarvis-voice.pid" "$HOME/start_with_voice.sh" "$HOME/stop_with_voice.sh" "$HOME/start_foreground_with_voice.sh" "$HOME/openjarvis_voice_wrapper.sh" "$HOME/openjarvis_voice_wake_patch.py" "$HOME/.openjarvis_voice_venv"; echo "reset-openjarvis: done"'
```
```
bash -lc 'set -euo pipefail; T="$HOME/Desktop/openjarvis"; if [ -e "$T" ] && [ -n "$(ls -A "$T" 2>/dev/null)" ]; then T="${T}-$(date +%Y%m%d_%H%M%S)"; fi; echo "Install path: $T"; mkdir -p "$T"; git clone https://github.com/open-jarvis/OpenJarvis.git "$T" >/dev/null 2>&1 || (echo "Repo already present or clone failed, using $T"); cd "$T"; echo "Creating isolated venv .venv..."; python3 -m venv .venv || true; . .venv/bin/activate; pip install --upgrade pip wheel setuptools >/dev/null 2>&1 || true; if [ -f requirements.txt ]; then echo "Installing Python requirements..."; pip install -r requirements.txt || true; else echo "No requirements.txt found, skipping pip install -r requirements.txt"; fi; if [ -d frontend ]; then if command -v npm >/dev/null 2>&1; then echo "Installing frontend dependencies..."; (cd frontend && npm install) || true; else echo "npm not found: install Node.js to build frontend"; fi; fi; echo "If the project requires Rust/native builds, ensure rustc/cargo are installed per OpenJarvis docs"; cat > "$T/start.sh" <<\'SH\'\n#!/usr/bin/env bash\nBASE_DIR=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\ncd \"$BASE_DIR\"\nLOG=\"$BASE_DIR/openjarvis.log\"\nPIDF=\"$BASE_DIR/openjarvis.pid\"\n# start backend (prefer uv if available, fallback to module in venv)\nif command -v uv >/dev/null 2>&1; then\n  nohup uv run jarvis serve --port 8000 >>\"$LOG\" 2>&1 & echo $! >\"$PIDF\"\nelse\n  nohup ./.venv/bin/python -m jarvis serve --port 8000 >>\"$LOG\" 2>&1 & echo $! >\"$PIDF\"\nfi\n# start frontend dev server if present\nif [ -d \"$BASE_DIR/frontend\" ] && command -v npm >/dev/null 2>&1; then\n  (cd frontend && nohup npm run dev >>\"$LOG\" 2>&1 &) || true\nfi\nsleep 1\n# open UI (default Vite dev port used by OpenJarvis frontend)\nopen \"http://127.0.0.1:5173/\" || true\necho \"Started. Backend PID: $(cat \"$PIDF\" 2>/dev/null)\"\nSH\nchmod +x \"$T/start.sh\"; cat > \"$T/stop.sh\" <<\'SH2\'\n#!/usr/bin/env bash\nBASE_DIR=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nPIDF=\"$BASE_DIR/openjarvis.pid\"\nif [ -f \"$PIDF\" ]; then\n  kill \"$(cat \"$PIDF\")\" 2>/dev/null || true\n  rm -f \"$PIDF\"\nfi\npkill -f \"npm run dev\" 2>/dev/null || true\npkill -f \"uv run jarvis\" 2>/dev/null || true\necho \"Stopped.\"\nSH2\nchmod +x \"$T/stop.sh\"; echo \"Done. Start with: $T/start.sh  Stop with: $T/stop.sh\"'
```
